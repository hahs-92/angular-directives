import { MsgErrDirective } from './msg-err.directive';

describe('MsgErrDirective', () => {
  it('should create an instance', () => {
    const directive = new MsgErrDirective();
    expect(directive).toBeTruthy();
  });
});
